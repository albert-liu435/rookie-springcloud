server:
  port: 10001 #指定运行端口
spring:
  application:
    name: eureka-consumer #指定服务名称
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://eureka-server.dev.com:8761/eureka}


logging:
  level:
    root: info